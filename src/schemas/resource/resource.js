const schema = {
  "$id": "/system/schema/database.document.resource",
  "title": "Database.Document.Resource",
  "allOf": [
    { "$ref": "/system/schema/database.document.resource.attributes" },
  ]
}

export default schema;
