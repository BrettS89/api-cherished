const schema = {
  "$id": "/system/schema/service.content.album.action.update",
  "title": "Service.Content.Album.Action.Create",
  "$ref": "/system/schema/service.content.album.resource",
};

export default schema;
