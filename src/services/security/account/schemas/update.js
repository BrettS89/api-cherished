const schema = {
  "$id": "/system/schema/service.security.account.action.update",
  "title": "Service.Security.Account.Action.Update",
  "$ref": "/system/schema/service.security.account.resource",
};

export default schema;
