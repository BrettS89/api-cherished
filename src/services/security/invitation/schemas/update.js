const schema = {
  "$id": "/system/schema/service.security.invitation.action.update",
  "title": "Service.Security.Invitation.Action.Update",
  "$ref": "/system/schema/service.security.invitation.resource"
};

export default schema;
