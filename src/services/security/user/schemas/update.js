const schema = {
  "$id": "/system/schema/service.security.user.action.update",
  "title": "Service.Security.User.Action.Update",
  "$ref": "/system/schema/service.security.user.resource"
};

export default schema;
